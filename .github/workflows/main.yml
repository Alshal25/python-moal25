name: Main Entry Workflow

on:
  pull_request:
    types: [opened, synchronize, closed]
  pull_request_review:
    types: [submitted]
  push:
    branches:
      - "bth/submit/*"
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Debug context
        run: |
          echo "github.ref: ${{ github.ref }}"
          echo "github.head_ref: ${{ github.head_ref }}"
          echo "github.base_ref: ${{ github.base_ref }}"
          echo "github.event_name: ${{ github.event_name }}"
          echo "event_name: ${{ github.event_name }}"
          echo "pr_action: ${{ github.event.action || '' }}"
          echo "review_state: ${{ github.event.review.state || '' }}"
  call-decider:
    uses: bth-python/utils/.github/workflows/decider.yml@main
    with:
      event_name: ${{ github.event_name }}
      event: ${{ toJson(github.event) }}
      pr_action: ${{ github.event.action || '' }}
      review_state: ${{ github.event.review.state || '' }}
      course_id: ${{ vars.COURSE_ID }}
      available_assignments: '{"bth/submit/kmom01":"kmom01","bth/submit/kmom02":"kmom02","bth/submit/kmom03":"kmom03","bth/submit/kmom04":"kmom04","bth/submit/kmom05":"kmom05","bth/submit/kmom06":"kmom06","bth/submit/proj":"proj"}'
      student_id: ${{ vars.STUDENT_CANVAS_ID }}
      kmom_paths: ${{ vars.KMOM_PATHS }}
    secrets: inherit
